import { TestSlideDemoQuestion } from './test-slide-demo-question';

describe('TestSlideDemoQuestion', () => {
  it('should create an instance', () => {
    expect(new TestSlideDemoQuestion()).toBeTruthy();
  });
});
