import { TestTranslatePipe } from './test-translate.pipe';

describe('TestTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new TestTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
